CMA-ES JSON Evaluator
Designed to evolve network in an environment where internal implementation is difficult.

Torch requirement assumes you have CUDA 12.4 so it's ok if pip -r requirements.txt errors. Just manually install the requirements.
